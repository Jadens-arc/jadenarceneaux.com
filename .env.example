# Backend (set these in the Vercel dashboard for the backend project)
SMTP_SERVER="smtp.example.com"
SMTP_PORT=587
SMTP_EMAIL="noreply@example.com"
SMTP_PASSWORD="yourpassword"
RECEIVER_EMAIL="youremailaddress"
TURNSTILE_SECRET_KEY="yourrecaptchasecret"
RATE_LIMIT=100

# Frontend (set these in the Vercel dashboard for the frontend project)
# Used at Hugo build time to point the contact form at the backend
API_ADDRESS="api.jadenarceneaux.com"
